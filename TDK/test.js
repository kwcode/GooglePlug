//出于安全考虑，javascript必须与html分开存放

 
var bg = chrome.extension.getBackgroundPage();
 chrome.tabs.getSelected(null, function (tab) {
     //var id = tab.id;
     //console.log(JSON.stringify(tab));
     //console.log(id);

 })
